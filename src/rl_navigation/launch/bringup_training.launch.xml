<launch>
  <!-- 环境变量 -->
  <set_env name="TURTLEBOT3_MODEL" value="burger"/>

  <!-- Gazebo -->
  <include file="$(find-pkg-share gazebo_ros)/launch/gazebo.launch.py">
    <arg name="world" value="$(find-pkg-share rl_navigation)/worlds/4_env.world"/>
  </include>

  <!-- robot_state_publisher -->
  <node pkg="robot_state_publisher" exec="robot_state_publisher" name="robot_state_publisher" output="screen">
    <!-- 用 command 调 cat，把 URDF 文件内容作为字符串写入 robot_description -->
    <param name="robot_description"
           value="$(command 'cat $(find-pkg-share turtlebot3_description)/urdf/turtlebot3_burger.urdf')"/>
    <param name="use_sim_time" value="true"/>
  </node>

  <!-- 训练节点（确保你已把 train_ddqn 安装为 console_script；节点名要与 online_config.yaml 的 publishers 一致：rl_env） -->
  <node pkg="rl_navigation" exec="train_ddqn" name="rl_env" output="screen">
    <param name="use_sim_time" value="true"/>
    <!-- <remap from="/cmd_vel" to="/cmd_vel_raw"/> -->
  </node>

  <node pkg="rl_navigation" exec="scan_sanitizer" name="scan_sanitizer" output="screen"/>

  <!-- Guard: allowed /cmd_vel_raw -> /cmd_vel; on violation -> turn -->
  <!-- <node pkg="rl_navigation" exec="cmd_guard" name="cmd_guard" output="screen"/> -->

</launch>
